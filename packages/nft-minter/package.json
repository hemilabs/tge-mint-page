{
  "name": "nft-minter",
  "version": "1.0.0",
  "scripts": {
    "fork:start": "npm run fork:stop && anvil --fork-url https://rpc.hemi.network/rpc &",
    "fork:stop": "lsof -ti:8545 | xargs kill || true",
    "pretest": "bash ./scripts/install-foundry",
    "test": "vitest run"
  },
  "devDependencies": {
    "p-wait-for": "5.0.2",
    "viem": "2.30.0",
    "vite": "6.3.5",
    "vitest": "3.1.4"
  },
  "peerDependencies": {
    "viem": ">=2.30.0"
  },
  "type": "module",
  "exports": {
    ".": "./src/mint.ts"
  }
}
